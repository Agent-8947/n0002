# DNA Matrix Production Export (SRC Structure)

Professional folder structure.

## Installation
1. `npm install`
2. `npm run dev`

## Critical Notes
- **Source**: All code is in `/src/`.
- **Components**: Copy your custom components to `/src/components/`.
