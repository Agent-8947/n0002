# DNA Matrix Production Export

This is a PRODUCTION-READY build.

## Installation
1. `npm install`
2. `npm run dev`

## Critical Notes
- **Components**: Only Base (Navbar, Hero) are included. Copy your custom components to `/components/`.
- **Registry**: The registry automagically handles imports.
